# 클라이언트에 노출되는 값만 VITE_ 접두사 사용
# 복사: cp .env.example .env

# 비우거나 생략하면 api.js 기본값 `/api` (Vite proxy → localhost:8080)
VITE_API_BASE_URL=/api

# 원격/다른 포트 예시
# VITE_API_BASE_URL=http://localhost:8080/api
